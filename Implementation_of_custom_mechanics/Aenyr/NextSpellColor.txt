# Реализация способности

**“Choose one or more colors. The next spell you cast this turn is the chosen color or colors in addition to its other colors.”**

## Цель

Добавить в движок Forge одноразовый эффект:

* игрок выбирает **1–5 цветов**;
* **следующее** разыгранное им заклинание в этот ход получает выбранный цвет или цвета **в дополнение** к своим цветам;
* эффект:

  * применяется **до выбора целей**;
  * учитывается **до SpellCast triggers**;
  * не расходуется, если каст был отменён или сорвался до успешного разыгрывания;
  * снимается при выходе заклинания со стека;
  * если заклинание не было сыграно — **сгорает в cleanup в конце хода**.

## Архитектурный принцип

Не добавлять отдельную логику проверки целей и не дублировать правила движка.

Механика должна использовать штатный механизм цвета карты:

```java
Card.addColor(ColorSet colors, true, timestamp, null)
```

Цвет должен применяться к карте-заклинанию до выбора целей. Тогда существующие проверки `SpellAbility.setupTargets()`, `SpellAbility.canTarget(...)`, `Card.isValid(...)`, `SpellCast`-триггеры и прочие штатные механизмы сами увидят правильное состояние spell-а.

Основная логика выносится в отдельный helper. Центральные flow изменяются только в точках, где это необходимо:

* `PlaySpellAbility` — Human/common cast-flow;
* `ComputerUtil` — AI cast-flow;
* `GameActionUtil.rollbackAbility` — общий rollback;
* `MagicStack` — cleanup при уходе со стека.


# Пример скрипта карты

```text
A:AB$ ChooseColorsForNextSpell | Cost$ T | SpellDescription$ Choose one or more colors. The next spell you cast this turn is the chosen color or colors in addition to its other colors.
```

Для spell-эффекта без активационной стоимости:

```text
A:SP$ ChooseColorsForNextSpell | SpellDescription$ Choose one or more colors. The next spell you cast this turn is the chosen color or colors in addition to its other colors.
```


---

# 1. Helper для применения цвета к заклинанию

**Новый файл:**

`forge-game/src/main/java/forge/game/spellability/NextSpellColorHelper.java`

**Создать файл с таким содержимым:**

```java
package forge.game.spellability;

import forge.card.ColorSet;
import forge.game.card.Card;
import forge.game.player.Player;

public final class NextSpellColorHelper {
    private NextSpellColorHelper() {}

    private static final String SVAR_NEXT_COLOR_MASK = "_NextSpellAddColorMask";
    private static final String SVAR_NEXT_COLOR_TS = "_NextSpellAddColorTS";

    private static boolean hasValue(final SpellAbility sa, final String key) {
        final String value = sa == null ? null : sa.getSVar(key);
        return value != null && !value.isEmpty();
    }

    /**
     * Prepare the next spell color effect before target selection.
     *
     * This does NOT consume the player's pending effect. The pending effect is
     * consumed only after the spell was successfully cast.
     */
    public static void prepareSpellColor(final Player caster, final SpellAbility sa) {
        if (caster == null || sa == null || !sa.isSpell() || sa.isCopied()) {
            return;
        }
        if (!caster.hasNextSpellAddColors()) {
            return;
        }
        if (hasValue(sa, SVAR_NEXT_COLOR_TS)) {
            return;
        }

        final ColorSet colors = caster.getNextSpellAddColors();
        if (colors == null || colors.isColorless()) {
            return;
        }

        final Card host = sa.getHostCard();
        if (host == null || host.getGame() == null) {
            return;
        }

        final long timestamp = host.getGame().getNextTimestamp();
        host.addColor(colors, true, timestamp, null);

        sa.setSVar(SVAR_NEXT_COLOR_MASK, Integer.toString(colors.getColor()));
        sa.setSVar(SVAR_NEXT_COLOR_TS, Long.toString(timestamp));
    }

    /**
     * Commit the prepared next-spell color effect after successful casting.
     *
     * The color remains on the spell while it is on the stack. Only the pending
     * player effect is cleared here.
     */
    public static void commitPreparedSpellColor(final Player caster, final SpellAbility sa) {
        if (caster == null || sa == null) {
            return;
        }
        if (!hasValue(sa, SVAR_NEXT_COLOR_MASK)) {
            return;
        }

        caster.clearNextSpellAddColors();
    }

    /**
     * Roll back a prepared color effect when casting fails or is cancelled.
     *
     * This does NOT clear the player's pending effect, because the spell was not
     * successfully cast.
     */
    public static void rollbackPreparedSpellColor(final SpellAbility sa) {
        clearDirectColorOverrideForSpellCast(sa);
    }

    /**
     * Remove the temporary color override when the spell leaves the stack.
     */
    public static void clearDirectColorOverrideForSpellCast(final SpellAbility sa) {
        if (sa == null) {
            return;
        }

        final String tsStr = sa.getSVar(SVAR_NEXT_COLOR_TS);
        if (tsStr != null && !tsStr.isEmpty()) {
            final Card host = sa.getHostCard();
            if (host != null) {
                try {
                    host.removeColor(Long.parseLong(tsStr), 0L);
                } catch (NumberFormatException ignored) {
                    // Ignore malformed internal state and still clear the markers below.
                }
            }
        }

        sa.setSVar(SVAR_NEXT_COLOR_TS, null);
        sa.setSVar(SVAR_NEXT_COLOR_MASK, null);
    }
}
```

---

# 2. Effect: выбор цветов

**Новый файл:**

`forge-game/src/main/java/forge/game/ability/effects/ChooseColorsForNextSpellEffect.java`

**Создать файл с таким содержимым:**

```java
package forge.game.ability.effects;

import forge.card.ColorSet;
import forge.card.MagicColor;
import forge.game.ability.SpellAbilityEffect;
import forge.game.player.Player;
import forge.game.spellability.SpellAbility;

public class ChooseColorsForNextSpellEffect extends SpellAbilityEffect {
    @Override
    public void resolve(final SpellAbility sa) {
        final Player player = sa.getActivatingPlayer();
        if (player == null) {
            return;
        }

        final ColorSet options = ColorSet.fromMask(
                MagicColor.WHITE
                        | MagicColor.BLUE
                        | MagicColor.BLACK
                        | MagicColor.RED
                        | MagicColor.GREEN
        );

        final ColorSet chosen = player.getController().chooseColors(
                "Choose one or more colors",
                sa,
                1,
                5,
                options
        );

        if (chosen == null || chosen.isColorless()) {
            return;
        }

        player.setNextSpellAddColors(chosen);
    }
}
```

---

# 3. Регистрация эффекта в ApiType

**Файл:**

`forge-game/src/main/java/forge/game/ability/ApiType.java`

В enum-списке рядом с `ChooseColor` добавить:

```java
ChooseColorsForNextSpell (ChooseColorsForNextSpellEffect.class),
```

Например:

```java
ChooseCard (ChooseCardEffect.class),
ChooseColor (ChooseColorEffect.class),
ChooseColorsForNextSpell (ChooseColorsForNextSpellEffect.class),
ChooseDirection (ChooseDirectionEffect.class),
```

---

# 4. Изменения в Player.java

**Файл:**

`forge-game/src/main/java/forge/game/player/Player.java`

## 4.1. Добавить хранение pending-цветов

**Место вставки:**

рядом с другими временными состояниями игрока, например рядом с блоком `pendingResonance` или похожими per-turn/pending-полями.

**Вставить:**

```java
// =========================================================
// Next spell additional colors (until end of turn)
// =========================================================
private forge.card.ColorSet nextSpellAddColors = null;

public void setNextSpellAddColors(final forge.card.ColorSet colors) {
    nextSpellAddColors = (colors == null || colors.isColorless()) ? null : colors;
}

public forge.card.ColorSet getNextSpellAddColors() {
    return nextSpellAddColors;
}

public boolean hasNextSpellAddColors() {
    return nextSpellAddColors != null && !nextSpellAddColors.isColorless();
}

public void clearNextSpellAddColors() {
    nextSpellAddColors = null;
}
```

Важно: здесь **не нужен** метод `consumeNextSpellAddColors()`.

Pending-эффект нельзя очищать в момент подготовки spell-а. Он должен очищаться только после успешного каста.

---

## 4.2. Очистка в cleanup

В `Player.java` найти метод:

```java
public void onCleanupPhase()
```

Внутри метода, рядом с другими сбросами состояния игрока за ход, добавить:

```java
clearNextSpellAddColors();
```

Например:

```java
public void onCleanupPhase() {
    clearNextSpellAddColors();

    ...
}
```

Очистка должна быть именно в cleanup, а не в `PlayerController.resetAtEndOfTurn()`. Текст эффекта говорит `this turn`, значит игрок должен иметь возможность использовать эффект до фактического конца хода, включая end step.

---

# 5. Изменения в PlaySpellAbility.java

**Файл:**

`forge-game/src/main/java/forge/game/player/PlaySpellAbility.java`

Этот файл отвечает за основной Human/common cast-flow.

Цвет должен примениться после того, как spell уже перенесён на stack zone и после всех замен/копирований `SpellAbility`, но **до** `ability.setupTargets()`.

---

## 5.1. Добавить импорт

В блок импортов добавить:

```java
import forge.game.spellability.NextSpellColorHelper;
```

Рядом с импортами:

```java
import forge.game.spellability.AlternativeCostCastProcessor;
import forge.game.spellability.OptionalCostValue;
import forge.game.spellability.ResonanceHelper;
import forge.game.spellability.SpellAbility;
```

---

## 5.2. Подготовить цвет до выбора целей

В методе:

```java
public final boolean playAbility(final boolean mayChooseTargets, final boolean isFree, final boolean skipStack)
```

найти блок:

```java
ability = AlternativeCostCastProcessor.process(ability);

Cost abCost = ability.getPayCosts();
CostPayment payment = new CostPayment(abCost, ability);
```

Заменить на:

```java
ability = AlternativeCostCastProcessor.process(ability);

if (!skipStack) {
    NextSpellColorHelper.prepareSpellColor(player, ability);
}

Cost abCost = ability.getPayCosts();
CostPayment payment = new CostPayment(abCost, ability);
```

Почему именно здесь:

* `GameActionUtil.addExtraKeywordCost(ability)` уже прошёл и мог заменить `ability`;
* `AlternativeCostCastProcessor.process(ability)` уже прошёл;
* `ability.setupTargets()` ещё не вызван;
* значит, target selection уже увидит новый цвет spell-а.

---

## 5.3. Зафиксировать pending-эффект после успешного каста

В том же методе найти блок:

```java
if (skipStack) {
    AbilityUtils.resolve(ability);
    // Should unfreeze stack (but if it was a RE with a cause better to let it be handled by that)
    if (!ability.isReplacementAbility()) {
        game.getStack().unfreezeStack();
    }
} else {
    ensureAbilityHasDescription(ability);
    game.getStack().addAndUnfreeze(ability);
}
```

Заменить на:

```java
if (skipStack) {
    AbilityUtils.resolve(ability);
    // Should unfreeze stack (but if it was a RE with a cause better to let it be handled by that)
    if (!ability.isReplacementAbility()) {
        game.getStack().unfreezeStack();
    }
} else {
    ensureAbilityHasDescription(ability);
    game.getStack().addAndUnfreeze(ability);
    NextSpellColorHelper.commitPreparedSpellColor(player, ability);
}
```

Commit очищает pending-эффект у игрока, но не снимает цвет со spell-а. Цвет должен оставаться на заклинании, пока оно находится на стеке.

Rollback при неудачном cast-flow будет выполнен через `GameActionUtil.rollbackAbility(...)`.

---

# 6. Изменения в GameActionUtil.java

**Файл:**

`forge-game/src/main/java/forge/game/GameActionUtil.java`

Метод `rollbackAbility(...)` — общая точка отката при отмене/провале каста. Нужно снять временный цвет с карты-заклинания, но **не очищать** pending-эффект игрока.

В файле уже есть импорт:

```java
import forge.game.spellability.*;
```

отдельный импорт для helper-а не нужен.

---

## 6.1. Добавить rollback цвета

Найти начало метода:

```java
public static void rollbackAbility(SpellAbility ability, final Zone fromZone, final int zonePosition, CostPayment payment, Card oldCard) {
    // cancel ability during target choosing
    final Game game = ability.getActivatingPlayer().getGame();

    if (game.restoreGameState()) {
```

Заменить на:

```java
public static void rollbackAbility(SpellAbility ability, final Zone fromZone, final int zonePosition, CostPayment payment, Card oldCard) {
    // cancel ability during target choosing
    final Game game = ability.getActivatingPlayer().getGame();

    NextSpellColorHelper.rollbackPreparedSpellColor(ability);

    if (game.restoreGameState()) {
```

Важно: rollback цвета должен быть до `game.restoreGameState()` и до замены host card обратно на старую карту.

---

# 7. Изменения в ComputerUtil.java

**Файл:**

`forge-ai/src/main/java/forge/ai/ComputerUtil.java`

AI имеет отдельные cast-flow, поэтому helper нужно подключить отдельно.

---

## 7.1. Добавить импорт

В блок импортов добавить:

```java
import forge.game.spellability.NextSpellColorHelper;
```

Рядом с импортами:

```java
import forge.game.spellability.AbilitySub;
import forge.game.spellability.SpellAbility;
import forge.game.spellability.SpellAbilityStackInstance;
import forge.game.spellability.TargetRestrictions;
```

---

## 7.2. Исправить handlePlayingSpellAbility(...)

Найти в методе:

```java
public static boolean handlePlayingSpellAbility(final Player ai, SpellAbility sa, Runnable chooseTargets)
```

блок:

```java
sa = GameActionUtil.addExtraKeywordCost(sa);

if (sa.getApi() == ApiType.Charm && !CharmEffect.makeChoices(sa)) {
    // 603.3c If no mode is chosen, the ability is removed from the stack.
    return false;
}
if (chooseTargets != null) {
    chooseTargets.run();
    if (!sa.isTargetNumberValid()) {
        return false;
    }
}
```

Заменить на:

```java
sa = GameActionUtil.addExtraKeywordCost(sa);

NextSpellColorHelper.prepareSpellColor(ai, sa);

if (sa.getApi() == ApiType.Charm && !CharmEffect.makeChoices(sa)) {
    // 603.3c If no mode is chosen, the ability is removed from the stack.
    NextSpellColorHelper.rollbackPreparedSpellColor(sa);
    return false;
}
if (chooseTargets != null) {
    chooseTargets.run();
    if (!sa.isTargetNumberValid()) {
        NextSpellColorHelper.rollbackPreparedSpellColor(sa);
        return false;
    }
}
```

---

## 7.3. Commit после успешной оплаты и добавления на стек

Найти ниже:

```java
if (pay.payComputerCosts(new AiCostDecision(ai, sa, false))) {
    game.getStack().addAndUnfreeze(sa);
    if (sa.getSplicedCards() != null && !sa.getSplicedCards().isEmpty()) {
        game.getAction().reveal(sa.getSplicedCards(), ai, true, "Computer reveals spliced cards from ");
    }
    return true;
}
```

Заменить на:

```java
if (pay.payComputerCosts(new AiCostDecision(ai, sa, false))) {
    game.getStack().addAndUnfreeze(sa);
    NextSpellColorHelper.commitPreparedSpellColor(ai, sa);

    if (sa.getSplicedCards() != null && !sa.getSplicedCards().isEmpty()) {
        game.getAction().reveal(sa.getSplicedCards(), ai, true, "Computer reveals spliced cards from ");
    }
    return true;
}
```

---

## 7.4. Rollback при failed payment

Ниже найти:

```java
// FIXME: Should not arrive here, though the card seems to be stuck on stack zone and invalidated and nowhere to be found, try to put back to original zone and maybe try to cast again if possible at later time?
System.out.println("[" + sa.getActivatingPlayer() + "] AI failed to play " + sa.getHostCard() + " [" + sa.getHostCard().getZone() + "]");
```

Перед `System.out.println(...)` вставить:

```java
NextSpellColorHelper.rollbackPreparedSpellColor(sa);
```

Должно получиться:

```java
NextSpellColorHelper.rollbackPreparedSpellColor(sa);

// FIXME: Should not arrive here, though the card seems to be stuck on stack zone and invalidated and nowhere to be found, try to put back to original zone and maybe try to cast again if possible at later time?
System.out.println("[" + sa.getActivatingPlayer() + "] AI failed to play " + sa.getHostCard() + " [" + sa.getHostCard().getZone() + "]");
```

---

## 7.5. Исправить playStack(...)

В методе:

```java
public static final boolean playStack(SpellAbility sa, final Player ai, final Game game)
```

найти блок:

```java
if (sa.isSpell() && !source.isCopiedSpell()) {
    sa.setHostCard(game.getAction().moveToStack(source, sa));
    sa = GameActionUtil.addExtraKeywordCost(sa);
}

final Cost cost = sa.getPayCosts();
final CostPayment pay = new CostPayment(cost, sa);
```

Заменить на:

```java
if (sa.isSpell() && !source.isCopiedSpell()) {
    sa.setHostCard(game.getAction().moveToStack(source, sa));
    sa = GameActionUtil.addExtraKeywordCost(sa);
}

NextSpellColorHelper.prepareSpellColor(ai, sa);

final Cost cost = sa.getPayCosts();
final CostPayment pay = new CostPayment(cost, sa);
```

Ниже найти:

```java
if (pay.payComputerCosts(new AiCostDecision(ai, sa, false))) {
    game.getStack().add(sa);
    return true;
}
return false;
```

Заменить на:

```java
if (pay.payComputerCosts(new AiCostDecision(ai, sa, false))) {
    game.getStack().add(sa);
    NextSpellColorHelper.commitPreparedSpellColor(ai, sa);
    return true;
}

NextSpellColorHelper.rollbackPreparedSpellColor(sa);
return false;
```

Блок проверки restrictions:

```java
if (!sa.checkRestrictions(ai)) {
    GameActionUtil.rollbackAbility(sa, fromZone, zonePosition, pay, source);
    return false;
}
```

дополнительно менять не нужно, потому что rollback цвета уже добавлен в `GameActionUtil.rollbackAbility(...)`.

---

# 8. Изменения в MagicStack.java

**Файл:**

`forge-game/src/main/java/forge/game/zone/MagicStack.java`

`MagicStack` больше не является местом, где цвет впервые применяется. Цвет уже должен быть применён до выбора целей в `PlaySpellAbility` / `ComputerUtil`.

Роль `MagicStack` в этой механике:

* safety-cleanup, если spell не удалось добавить на стек;
* cleanup при нормальном разрешении/fizzle;
* cleanup при counter/remove со стека.

---

## 8.1. Добавить импорт

В блок импортов добавить:

```java
import forge.game.spellability.NextSpellColorHelper;
```

Рядом с импортами spellability-классов.

---

## 8.2. Не добавлять применение цвета в MagicStack.add(...)

В метод:

```java
public final void add(SpellAbility sp, SpellAbilityStackInstance si, int id)
```

**не нужно** добавлять `tagSpellOnCast(...)` перед `// CHECK TARGETING`.

**Не нужно** добавлять `applyDirectColorOverrideForSpellCast(...)` после `sp.applyPayingManaEffects()`.

Это важное отличие от ранней версии механики. `MagicStack.add(...)` слишком поздний для выбора целей.

---

## 8.3. Safety-cleanup при failed targeting в add(...)

В методе:

```java
public final void add(SpellAbility sp, SpellAbilityStackInstance si, int id)
```

найти блок:

```java
// CHECK TARGETING
if (!sp.isCopied() && !hasLegalTargeting(sp)) {
    String str = source + " - [Couldn't add to stack, failed to target] - " + sp.getDescription();
    System.err.println(str + sp.getAllTargetChoices());
    game.fireEvent(new GameEventAddLog(GameLogEntryType.STACK_ADD, str));
    return;
}
```

Заменить на:

```java
// CHECK TARGETING
if (!sp.isCopied() && !hasLegalTargeting(sp)) {
    String str = source + " - [Couldn't add to stack, failed to target] - " + sp.getDescription();
    System.err.println(str + sp.getAllTargetChoices());
    game.fireEvent(new GameEventAddLog(GameLogEntryType.STACK_ADD, str));
    NextSpellColorHelper.clearDirectColorOverrideForSpellCast(sp);
    return;
}
```

---

## 8.4. Cleanup при нормальном уходе со стека

Найти метод:

```java
private void removeCardFromStack(final SpellAbility sa, final SpellAbilityStackInstance si, final boolean fizzle)
```

В начале метода, сразу после:

```java
Card source = sa.getHostCard();
```

вставить:

```java
// ===== Next spell color cleanup =====
NextSpellColorHelper.clearDirectColorOverrideForSpellCast(sa);
```

Должно получиться примерно так:

```java
private void removeCardFromStack(final SpellAbility sa, final SpellAbilityStackInstance si, final boolean fizzle) {
    Card source = sa.getHostCard();

    // ===== Next spell color cleanup =====
    NextSpellColorHelper.clearDirectColorOverrideForSpellCast(sa);

    // дальше существующий код метода
    ...
}
```

Если рядом уже есть cleanup Resonance — оставить его, не удалять.

---

## 8.5. Cleanup при прямом удалении stack instance

Найти метод:

```java
public final void remove(final SpellAbilityStackInstance si)
```

В чистом исходнике он выглядит примерно так:

```java
public final void remove(final SpellAbilityStackInstance si) {
    stack.remove(si);
    frozenStack.remove(si);
    game.updateStackForView();
    game.fireEvent(new GameEventSpellRemovedFromStack(SpellAbilityView.get(si.getSpellAbility())));
}
```

Заменить на:

```java
public final void remove(final SpellAbilityStackInstance si) {
    NextSpellColorHelper.clearDirectColorOverrideForSpellCast(si.getSpellAbility());

    stack.remove(si);
    frozenStack.remove(si);
    game.updateStackForView();
    game.fireEvent(new GameEventSpellRemovedFromStack(SpellAbilityView.get(si.getSpellAbility())));
}
```

Это важно для counter/remove со стека. `CounterEffect` удаляет заклинание через `game.getStack().remove(si)`, а уже потом перемещает карту в graveyard/exile/hand/library/battlefield.

---

# 9. Изменения в ComputerUtilCard.java

**Файл:**

`forge-ai/src/main/java/forge/ai/ComputerUtilCard.java`

AI уже вызывает общий метод выбора нескольких цветов через `PlayerControllerAi.chooseColors(...)`, но дальше он делегирует в `ComputerUtilCard.chooseColor(...)`. В большинстве старых веток `chooseColor(...)` возвращает только один цвет. Для этой механики нужно разрешить AI выбрать несколько цветов, но не ломать общий выбор цветов для других карт.

---

## 9.1. Изменить финальный return в chooseColor(...)

Найти метод:

```java
public static List<String> chooseColor(SpellAbility sa, int min, int max, List<String> colorChoices)
```

В конце метода найти блок:

```java
if (chosen.isEmpty()) {
    //chosen.add(MagicColor.Constant.GREEN);
    chosen.add(getMostProminentColor(ai.getAllCards(), colorChoices));
}
return chosen;
```

Заменить на:

```java
if (chosen.isEmpty()) {
    //chosen.add(MagicColor.Constant.GREEN);
    chosen.add(getMostProminentColor(ai.getAllCards(), colorChoices));
}

if (ApiType.ChooseColorsForNextSpell.equals(sa.getApi()) && max > 1) {
    addMostProminentColors(
            chosen,
            getColorByProminence(CardLists.filterControlledBy(game.getCardsInGame(), ai)),
            colorChoices,
            max
    );
}

return normalizeColorChoiceList(chosen, colorChoices, min, max);
```

---

## 9.2. Добавить helper-методы ниже chooseColor(...)

Сразу после метода `chooseColor(...)` добавить:

```java
private static void addMostProminentColors(final List<String> chosen, final List<String> prominence,
                                           final List<String> colorChoices, final int max) {
    for (final String color : prominence) {
        if (chosen.size() >= max) {
            return;
        }
        addColorChoice(chosen, color, colorChoices, max);
    }
}

private static List<String> normalizeColorChoiceList(final List<String> chosen, final List<String> colorChoices,
                                                     final int min, final int max) {
    final int maxChoices = Math.max(0, Math.min(max, colorChoices.size()));
    final int minChoices = Math.max(0, Math.min(min, maxChoices));

    final List<String> result = new ArrayList<>();
    for (final String color : chosen) {
        addColorChoice(result, color, colorChoices, maxChoices);
    }

    if (result.isEmpty() && maxChoices > 0) {
        result.add(colorChoices.get(0));
    }

    for (final String color : colorChoices) {
        if (result.size() >= minChoices) {
            break;
        }
        addColorChoice(result, color, colorChoices, maxChoices);
    }

    return result;
}

private static void addColorChoice(final List<String> chosen, final String color,
                                   final List<String> colorChoices, final int max) {
    if (chosen.size() >= max) {
        return;
    }

    final String allowed = getAllowedColorChoice(colorChoices, color);
    if (allowed == null || containsColorChoice(chosen, allowed)) {
        return;
    }

    chosen.add(allowed);
}

private static String getAllowedColorChoice(final List<String> colorChoices, final String color) {
    if (color == null) {
        return null;
    }

    for (final String choice : colorChoices) {
        if (choice.equalsIgnoreCase(color)) {
            return choice;
        }
    }
    return null;
}

private static boolean containsColorChoice(final List<String> colors, final String color) {
    for (final String existing : colors) {
        if (existing.equalsIgnoreCase(color)) {
            return true;
        }
    }
    return false;
}
```

`ComputerUtilCard.java` уже использует `ArrayList` и `List`, поэтому дополнительных импортов обычно не требуется.

---

# 10. Что НЕ менять

## Не менять SpellAbility.canTarget(...)

Не добавлять в `SpellAbility.canTarget(...)` отдельные проверки для этой механики.

Правильная модель: spell уже имеет нужный цвет до выбора целей, поэтому штатный targeting-код сам видит правильный `Card.getColor()`.

## Не менять GameAction.changeZone(...)

Zone-change не должен знать о кастомной механике цвета. Cleanup находится в stack-flow.

## Не менять CounterEffect.java

Counter уже удаляет spell через `MagicStack.remove(si)`. Cleanup добавлен туда.

## Не чистить pending в PlayerControllerHuman / PlayerControllerAi

Не добавлять:

```java
player.clearNextSpellAddColors();
```

в `resetAtEndOfTurn()`.

Очистка должна быть в `Player.onCleanupPhase()`.

---


